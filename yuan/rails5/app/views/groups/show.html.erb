<div class="col-md-12">
  <div class="group">
    <%= link_to("write a Post", new_group_post_path(@group), class: "btn btn-default pull-right") %>
    <%= link_to("Edit", edit_group_path(@group), class: "ctn ctn-primary pull-right") %>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
